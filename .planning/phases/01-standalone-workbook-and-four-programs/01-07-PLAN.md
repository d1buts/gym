---
phase: 01-standalone-workbook-and-four-programs
plan: 07
type: execute
wave: 7
depends_on:
  - 01-03
  - 01-05
  - 01-06
files_modified:
  - src/workout_tracker/workbook/reconcile.py
  - src/workout_tracker/adapters/memory.py
  - tests/contract/test_reconcile_idempotency.py
autonomous: true
requirements:
  - REQ-WBK-05
must_haves:
  truths:
    - "A clean in-memory setup allocates UUID4 values only for missing logical reserve slots, reaches the exact desired logical workbook, and a second full compile/observe/reconcile cycle has zero operations with the same persisted reserve IDs and canonical managed hash."
    - "No second run duplicates a tab, named range, formula, protection, chart, filter view, reserve ID, or program item."
    - "Partial managed drift is repaired by logical key while occupied IDs, used program versions, and unowned objects are preserved or reported as explicit conflicts."
    - "The observed managed fingerprint is an apply precondition, and stale state fails closed."
  artifacts:
    - path: "src/workout_tracker/workbook/reconcile.py"
      provides: "Pure canonical observe/diff/plan and no-op proof"
      exports: ["canonicalize_managed_state", "plan_setup"]
    - path: "src/workout_tracker/adapters/memory.py"
      provides: "Full immutable ChangePlan executor for offline proof"
    - path: "tests/contract/test_reconcile_idempotency.py"
      provides: "Clean/partial/correct/unowned/drift/second-run matrix"
  key_links:
    - from: "src/workout_tracker/workbook/reconcile.py"
      to: "src/workout_tracker/workbook/model.py"
      via: "compares DesiredWorkbook to normalized ObservedWorkbook by logical key"
      pattern: "plan_setup"
    - from: "tests/contract/test_reconcile_idempotency.py"
      to: "src/workout_tracker/adapters/memory.py"
      via: "first apply then re-observe and assert exact empty second ChangePlan"
      pattern: "InMemoryWorkbookGateway"
---

<objective>
Prove true repeatable setup and second-run idempotency for REQ-WBK-05 entirely offline.

Purpose: Make the requirement independent of Google credentials while establishing the same deterministic ChangePlan later consumed by the Google adapter.
Output: Pure reconciler, complete in-memory executor, and canonical idempotency matrix.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@docs/specs/SPEC-GOOGLE-SHEETS-WORKBOOK.md
@config/workbook-blueprint.yaml
@config/program-bootstrap.yaml
@.planning/phases/01-standalone-workbook-and-four-programs/01-03-SUMMARY.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-05-SUMMARY.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-06-SUMMARY.md
</read_first>

<context>
Implement D-14 and D-15. This plan is the sole frontmatter owner of REQ-WBK-05. Plans 01-08 and 01-09 support its transport/live evidence with `requirements: []`.

<interfaces>
Use:
- `compile_desired_workbook(...) -> DesiredWorkbook`
- `WorkbookGateway.observe() -> ObservedWorkbook`
- `WorkbookGateway.apply(ChangePlan) -> ApplyResult`

Create `plan_setup(desired, observed) -> ChangePlan`. A successful `ApplyResult` includes before/after managed fingerprints, operation counts/kinds, and a redacted status only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and prove pure second-run reconciliation</name>
  <files>src/workout_tracker/workbook/reconcile.py, src/workout_tracker/adapters/memory.py, tests/contract/test_reconcile_idempotency.py</files>
  <behavior>
    - Clean, partial, already-correct, managed-drift, unowned-object, and stale-fingerprint inputs yield deterministic ordered plans.
    - Applying a clean plan to memory and planning again yields zero operations and the identical canonical managed hash.
    - Desired reserve slots carry no UUID values; apply allocates RFC UUID4 values only for missing observed slots.
    - No duplicate managed key, program item, or reserve ID appears; occupied and still-reserved persisted IDs plus used program versions are never regenerated.
    - Unowned objects are preserved or return explicit conflict; no broad delete operation is produced.
  </behavior>
  <red_evidence>RED is valid only after the intended tests collect and fail on an assertion for the specifically missing reconciliation behavior. Unavailable tools, import or collection errors, malformed fixtures/configuration, and unrelated failures are setup defects that must be corrected before implementation continues.</red_evidence>
  <action>Write the full matrix first, then implement canonicalization over only allowlisted managed fields and a pure logical-key diff. Desired state supplies logical reserve slots/capacity without UUID values; observed state supplies any persisted slot-to-ID bindings. Emit a typed `AllocateReserveId` only for each observed missing slot, leaving the pure plan deterministic; resolve that operation at gateway apply with an injectable secure UUID4 generator, persist the value once, and never allocate for occupied or still-reserved slots. Order changes by dependency: workbook properties/tabs; headers/program/lookups/reserve allocation; named ranges; validations/formats/formulas; protections/filter views/conditional rules; dashboard charts; verification. Generate typed Add, Update, allocation, narrowly owner-scoped removal, or NoOp changes; provider IDs remain observed attributes. Require the observed fingerprint as an apply precondition. Complete the in-memory gateway so it executes the exact plan into immutable snapshots. Run two complete setup cycles—compile desired, observe, reconcile, apply, re-observe, then compile desired again—and assert every reserve ID is identical, the second ChangePlan has zero operations, and the canonical logical hash is unchanged.</action>
  <verify>
    <automated>uv run pytest -q tests/contract/test_reconcile_idempotency.py tests/contract/test_gateway_contract.py</automated>
  </verify>
  <done>Offline setup is deterministic, non-destructive, drift-aware, and yields an exact empty plan on its second run.</done>
  <acceptance_criteria>
    - First and second post-state logical hashes match.
    - Second operation count is exactly zero.
    - Unmanaged objects, occupied IDs, and immutable used versions are never silently removed or replaced.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| observed state → pure reconciler | External-like state may be stale, malformed, drifted or unowned |
| ChangePlan → gateway apply | Planned changes become managed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-18 | Tampering/Repudiation | stale observed state | high | mitigate | managed pre-fingerprint is mandatory and mismatch fails before apply |
| T-01-19 | Tampering/Denial | unowned objects | medium | mitigate | owner-key allowlist, explicit conflict, and no broad delete variant |
| T-01-20 | Spoofing | reserve/program identity | high | mitigate | pure logical-slot diff, apply-time secure UUID4 allocation only for deficits, persisted-binding reuse, and preservation of immutable used program versions |
</threat_model>

<verification>
Run `uv run pytest -q tests/contract/test_reconcile_idempotency.py tests/contract/test_gateway_contract.py`, parse all YAML, and run `git diff --check`.
</verification>

<success_criteria>
- REQ-WBK-05 passes without external state through an empty second ChangePlan and unchanged hash.
- Clean, partial, correct, drifted, and unowned states cannot create duplicates or arbitrary deletion.
</success_criteria>

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `src/workout_tracker/workbook/reconcile.py` | `canonicalize_managed_state`, typed change variants and `plan_setup` |
| `src/workout_tracker/adapters/memory.py` | complete immutable ChangePlan execution |
| `tests/contract/test_reconcile_idempotency.py` | offline clean/partial/correct/drift/unowned/second-run proof |

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-07-SUMMARY.md` when done.
</output>
