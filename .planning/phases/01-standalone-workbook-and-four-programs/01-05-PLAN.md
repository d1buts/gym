---
phase: 01-standalone-workbook-and-four-programs
plan: 05
type: execute
wave: 5
depends_on:
  - 01-03
  - 01-04
files_modified:
  - config/workbook-blueprint.yaml
  - src/workout_tracker/workbook/__init__.py
  - src/workout_tracker/workbook/model.py
  - tests/contract/test_input_contract.py
  - tests/unit/test_workbook_model.py
autonomous: true
requirements:
  - REQ-WBK-03
must_haves:
  truths:
    - "From a narrow mobile view, the user can see the next complex, last completed session, four one-tap complex links, short manual guidance, and redacted sync/backup status on Старт."
    - "The user can activate a session before child sets and enter facts through input-first append zones linked only by stable session_id, without editing protected formula/system columns."
    - "Finite enums, dates/times, numeric ranges, cross-field requirements, hints, formats, and visible text/symbol statuses match config/schema.yaml; permitted unknown stays blank/NULL."
    - "Headers are frozen, input/system zones are visually distinct, no decorative merged cells are used, and primary mobile actions do not require horizontal scrolling."
  artifacts:
    - path: "config/workbook-blueprint.yaml"
      provides: "Complete manual-entry, navigation, validation, formatting, protection, and named-range desired state"
      contains: "managed_input_zones"
    - path: "src/workout_tracker/workbook/model.py"
      provides: "Pure compiler from repository contracts to DesiredWorkbook"
      exports: ["DesiredWorkbook", "compile_desired_workbook"]
    - path: "tests/contract/test_input_contract.py"
      provides: "Schema-to-Sheet validation/protection/mobile contract"
  key_links:
    - from: "src/workout_tracker/workbook/model.py"
      to: "config/schema.yaml"
      via: "derives exact headers, field roles, validation bounds and data classes"
      pattern: "load_source_schema"
    - from: "src/workout_tracker/workbook/model.py"
      to: "config/program-bootstrap.yaml"
      via: "builds complex links/filter views and program rows"
      pattern: "load_program_bootstrap"
    - from: "config/workbook-blueprint.yaml"
      to: "tests/contract/test_input_contract.py"
      via: "asserts named ranges, protected zones, widths, hints and status rules"
      pattern: "protection"
---

<objective>
Deliver the mobile-first manual workbook experience, validations, named ranges, and protections for REQ-WBK-03.

Purpose: Make the Sheet independently usable for daily planning and factual entry while keeping identity, NULL, and system-column rules intact.
Output: Complete presentation/input blueprint and a pure compiled desired-workbook model.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@docs/specs/SPEC-GOOGLE-SHEETS-WORKBOOK.md
@docs/architecture/DATA_MODEL.md
@config/schema.yaml
@.planning/phases/01-standalone-workbook-and-four-programs/01-03-SUMMARY.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-04-SUMMARY.md
</read_first>

<context>
Implement D-01 through D-15 where they govern manual UX. The initial workbook locale is exactly `uk_UA` and timezone is exactly `America/New_York` per D-13; neither may be inferred from the developer host. Offline fixtures use these explicit non-personal properties, and Plan 01-09 verifies them live.

<interfaces>
From prior plans:
- `WorkbookBlueprint`, `SourceSchema`, `ProgramBootstrap`
- `ObservedWorkbook`, `ChangePlan`
- exact source columns and `data_class` values from `config/schema.yaml`

Create `compile_desired_workbook(blueprint, schema, program, *, locale, time_zone) -> DesiredWorkbook`. It is pure and does not import Google APIs.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify mobile navigation and safe manual-entry contracts</name>
  <files>tests/contract/test_input_contract.py, config/workbook-blueprint.yaml</files>
  <behavior>
    - `Старт` contains D-01 content and four links target distinct managed filter views in one tap.
    - `Сесії` and `Підходи` place editable columns before separated protected/system columns and preserve exact schema headers.
    - Every finite enum, type/range, required/nullable rule, help note, number/date format, conditional status, named range, freeze, filter view, width, and protection is declared once.
    - Missing nullable values remain blank; critical missing values display an explicit Ukrainian invalid status without inserting zero.
  </behavior>
  <action>Write the contract assertions, then extend the blueprint with a light high-contrast palette and four consistent workout accents per D-05, always paired with text/symbol per D-06. Define concrete column widths and frozen regions so the primary entry sequence and `Старт` cards fit a narrow viewport without decorative merges. Create stable named ranges for lookup enums and active program data; validation rules must reference these ranges or exact schema bounds and include Ukrainian help text. Use strict-reject validation where safe, visible quality diagnostics for cross-row constraints, and real protections (not warning-only) for IDs/formulas/system fields.</action>
  <verify>
    <automated>uv run pytest -q tests/contract/test_input_contract.py</automated>
  </verify>
  <done>The blueprint fully and concretely specifies mobile navigation, validations, hints, formats, named ranges, filter views, conditional statuses, and protections.</done>
  <acceptance_criteria>
    - Exact Sheet titles/headers and Ukrainian user-facing labels are preserved.
    - No status relies on color alone and no decorative merged cell is declared.
    - All system/formula ranges use enforced protections.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compile a complete desired workbook without Google</name>
  <files>src/workout_tracker/workbook/__init__.py, src/workout_tracker/workbook/model.py, tests/unit/test_workbook_model.py, tests/contract/test_input_contract.py</files>
  <behavior>
    - The compiler resolves schema headers/roles, 31 program rows, lookup ranges, filter links, validation rules, formats and protections to logical-keyed objects.
    - Locale and timezone are mandatory explicit inputs whose initial values are exactly uk_UA and America/New_York.
    - Managed reserve IDs are UUID4 values persisted once as values, not formulas; repeated compile/observe preserves them and sorting/factual correction cannot change identity.
    - A child set cannot be activated without selecting an existing session_id and matching program_version_id.
  </behavior>
  <action>Implement immutable `DesiredWorkbook` and `compile_desired_workbook`. Require locale/timezone inputs and validate them against the blueprint properties; assert exact `uk_UA` and `America/New_York` under D-13 and never read host locale/timezone. Compile all objects by stable logical key, never index/row identity. To preserve standalone manual use under D-04 and D-11, represent bounded managed reserve rows for sessions/sets: setup generates cryptographically random UUID4 source IDs once, persists them as protected values, and later reconciliation preserves occupied or still-reserved IDs. A row becomes an active factual record only through the managed activation state; session activation precedes set activation, and set validation references active `session_id` values. Replenishment is an explicit managed setup operation, not a volatile formula. Tests must prove IDs survive sort, correction, and repeated compile/reconcile; blank facts stay NULL and are not counted as sessions/sets.</action>
  <verify>
    <automated>uv run pytest -q tests/unit/test_workbook_model.py tests/contract/test_input_contract.py</automated>
  </verify>
  <done>A complete credential-free desired workbook can be compiled, including safe stable-ID manual entry and every UX object required by the blueprint.</done>
  <acceptance_criteria>
    - Pure compilation performs no environment lookup or network call.
    - `session_id`, not row number, is the only session/set link.
    - Reserve IDs are one-time values and inactive reserve rows are not operational facts.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user mobile input → authoritative tabs | Untrusted facts cross validation/protection controls |
| schema/program/blueprint → DesiredWorkbook | Repository contracts are compiled into managed objects |
| managed ID reserve → active record | Identity must be immutable and non-positional |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | manual system/formula columns | high | mitigate | enforced protected ranges, input-first field allowlist, and contract tests |
| T-01-09 | Spoofing | session/set identity | high | mitigate | one-time UUID4 value generation, protected persistence, active-session foreign-key validation, no row-number fallback |
| T-01-10 | Tampering | validation source/ranges | medium | mitigate | stable named ranges, schema-derived bounds, strict cross-contract tests |
| T-01-11 | Information Disclosure | Старт status | medium | mitigate | expose redacted status/version/count only; no locator, credential path, notes, or health content |
| T-01-12 | Denial | protection/layout drift | low | accept | drift is visible in later reconciliation and mobile UAT; no security claim is assigned to protection |
</threat_model>

<verification>
Run `uv run pytest -q tests/unit/test_workbook_model.py tests/contract/test_input_contract.py`, parse all YAML, and run `git diff --check`.
</verification>

<success_criteria>
- REQ-WBK-03 is fully specified and proven through compiled offline state.
- Manual session/set entry preserves source identity, NULL, ordering, and foreign-key rules.
- Mobile navigation, validations, protections, and visual rules are concrete enough for live UAT.
</success_criteria>

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `src/workout_tracker/workbook/__init__.py` | workbook public exports |
| `src/workout_tracker/workbook/model.py` | `DesiredWorkbook`, managed object models, `compile_desired_workbook`, reserve-ID activation rules |
| `tests/contract/test_input_contract.py` | navigation/input/validation/protection contract |
| `tests/unit/test_workbook_model.py` | pure compilation and identity stability tests |

This plan also expands `config/workbook-blueprint.yaml` with navigation, mobile layout, lookup ranges, filters, managed input zones, validation, formatting, conditional status, and protections.

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-05-SUMMARY.md` when done.
</output>
