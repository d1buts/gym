---
context: phase
phase: 01-standalone-workbook-and-four-programs
task: 0
total_tasks: 0
status: in_progress
last_updated: 2026-07-30T21:30:15.657Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through
> failure. Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Plans are not checker-approved — the first plan-checker found
  6 blockers and 2 warnings; revision was interrupted for this portable
  snapshot before the second pass.
- [ ] CONSTRAINT: Exact-once requirement ownership — current `01-09-PLAN.md`
  still duplicates `REQ-WBK-05`, which belongs to `01-07-PLAN.md`.
- [ ] CONSTRAINT: Live evidence cannot be invented — disposable Google test
  Sheet and local OAuth are absent from Git and may remain unavailable until
  final UAT.

**Do not begin execution until all boxes are checked and the plan-checker
returns `## VERIFICATION PASSED`.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Execute interrupted plan revision | Nine PLAN files exist, but `01-09` is still a superseded/overlapping draft | blocking | Finish revision, ensure exact-once requirements and rerun `gsd-plan-checker` |
| Wait indefinitely on optional agent | Pattern mapper failed with external 503 and produced no artifact | advisory | Treat mapper as non-blocking; use CONTEXT/RESEARCH/contracts |
| Treat skipped UAT as success | Plain pytest can exit zero when all `google_uat` cases skip | blocking | Wrapper/preflight must require non-zero executed live-test count |
| Log or commit Google identifiers | Repository has known public-history disclosure | blocking | Keep credentials/locator outside Git and scan staged text before commit |

<current_state>

Phase 1 is researched and partially planned. No product source code exists.
Context, research and validation are complete. The initial five-plan proposal
failed checker review; a revision split it into nine plans, but the revision
agent was interrupted at the user's request to commit and push a portable
snapshot.

Plans `01-01` through `01-08` represent the intended split:

1. uv/toolchain and dependency policy;
2. strict contracts;
3. exact seven-tab topology (`REQ-WBK-01`);
4. four-program bootstrap (`REQ-WBK-02`);
5. mobile input/protections (`REQ-WBK-03`);
6. formulas/dashboard (`REQ-WBK-04`);
7. offline reconciliation/idempotency (`REQ-WBK-05`);
8. Google adapter and guarded CLI support.

`01-09-PLAN.md` is incomplete WIP. It still has wave 5, stale dependencies,
overlapping source files and duplicate `REQ-WBK-05`. Convert it to a final
wave-9 live-UAT/checkpoint support plan with `requirements: []`, depending on
`01-08`, and only UAT/evidence ownership; or replace it with an equivalent
clean plan.

</current_state>

<completed_work>

- Spreadsheet-first ADR/PRD/specs and architecture rebaseline committed.
- GSD ingest of 21 sources: 0 blockers, 0 warnings.
- New roadmap: 5 phases, 26/26 requirements exact-once.
- Phase 1 smart-discuss context completed.
- Phase 1 implementation research completed.
- Former open questions resolved:
  - Dumbbell Romanian deadlift;
  - locale `uk_UA`;
  - timezone `America/New_York`;
  - installed-app OAuth for disposable live UAT;
  - canonical dependency set approved.
- Phase 1 Nyquist validation strategy completed.
- Initial plan-checker report captured in the conversation and addressed
  partially by the interrupted revision.

</completed_work>

<remaining_work>

1. Finish coherent PLAN.md revision.
2. Run independent `gsd-plan-checker`; revise until PASS.
3. Update STATE/ROADMAP planning status and commit verified plans.
4. Execute offline Phase 1 plans.
5. Run automated verification.
6. Perform disposable Google Sheet UAT when safe local authorization exists.
7. Continue autonomous Phases 2–5.

</remaining_work>

<decisions_made>

- Google Spreadsheet is the primary standalone product.
- Exact workbook tabs: `Старт`, `Програма`, `Сесії`, `Підходи`,
  `Рекомендації`, `Довідники`, `Дашборд`.
- Manual entry is append-only and stable-ID based.
- Lower Hypertrophy C1 is Dumbbell Romanian deadlift.
- Locale/timezone are `uk_UA` and `America/New_York`.
- Live Phase 1 UAT uses test-only installed-app OAuth and a disposable target.
- Canonical dependencies are approved through uv; source/name drift fails
  closed.

</decisions_made>

<blockers>

- Planning gate: current plans have not passed the second checker.
- Later human action: disposable Google test Sheet and local OAuth are needed
  only for final live UAT.

</blockers>

## Required Reading (in order)

1. `AGENTS.md` — repository-wide safety and workflow rules.
2. `.planning/HANDOFF.json` — machine-readable pause state.
3. `.planning/PROJECT.md` — locked product decisions.
4. `.planning/REQUIREMENTS.md` — exact requirement ownership.
5. `.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md`
   — user-approved implementation decisions.
6. `.planning/phases/01-standalone-workbook-and-four-programs/01-RESEARCH.md`
   — implementation and validation findings.
7. `.planning/phases/01-standalone-workbook-and-four-programs/01-VALIDATION.md`
   — automated and manual evidence contract.
8. All `01-*-PLAN.md` files — interrupted revision snapshot.

## Critical Anti-Patterns (do NOT repeat these)

- Do not execute merely because PLAN.md files exist; require checker PASS.
- Do not duplicate one requirement across support plans.
- Do not accept any pytest failure as a valid RED test.
- Do not accept zero executed live tests as UAT success.
- Do not omit manual cards/chart/filter/NULL-gap verification.
- Do not commit live identifiers, credentials or personal workout data.

## Infrastructure State

- Branch: `agent/workout-tracker-architecture`.
- Draft PR: GitHub PR #1.
- No servers, watchers or async jobs are running.
- No source package or product code has been implemented.
- `uv` was absent during research; Plan 01-01 must bootstrap/preflight it.
- Optional pattern mapper failed with external HTTP 503; no PATTERNS.md exists.

## Pre-Execution Critique Required

- Design artifacts: all `01-*-PLAN.md`.
- Critique focus: exact-once requirement ownership, plan 01-09 cleanup,
  dependency waves/file ownership, non-skipped live UAT proof, manual dashboard
  rendering evidence and package/tool bootstrap.
- Gate: Do NOT begin execution until `gsd-plan-checker` returns
  `## VERIFICATION PASSED`.

<context>

The work paused intentionally so it can continue on another PC. The correct
next move is plan repair and verification, not implementation. All
authoritative decisions and research are already present locally and will be
pushed with this handoff.

</context>

<next_action>

Start with: `$gsd-resume-work`, then finish/recheck Phase 1 planning.

</next_action>
