---
phase: 01-standalone-workbook-and-four-programs
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - config/schema.yaml
  - config/program-bootstrap.yaml
  - docs/architecture/DATA_MODEL.md
  - src/workout_tracker/contracts/program.py
  - tests/contract/test_program_bootstrap.py
autonomous: true
requirements:
  - REQ-WBK-02
must_haves:
  truths:
    - "The bootstrap contains exactly four workout complexes and exactly 31 prescription rows: 8 upper-strength, 7 lower-strength, 8 upper-hypertrophy, and 8 lower-hypertrophy."
    - "Every prescription preserves block/pair order, sets, repetition or duration semantics, RIR, known rest, optionality, laterality, exact variant, and source-known equipment/setup/cohort; source-unknown prescribed rest, equipment, setup and dependent cohort remain NULL."
    - "Lower Hypertrophy C1 is exactly Dumbbell Romanian deadlift; a barbell substitution requires another program version and cohort."
    - "Initial program and item IDs are stable source-owned values and definition drift cannot mutate an already-used version."
  artifacts:
    - path: "config/program-bootstrap.yaml"
      provides: "Audited 31-row bootstrap transcription with source locators and definition hashes"
      contains: "Dumbbell Romanian deadlift"
    - path: "src/workout_tracker/contracts/program.py"
      provides: "Strict program bootstrap loader and cross-contract validator"
      exports: ["ProgramBootstrap", "ProgramPrescription", "load_program_bootstrap"]
    - path: "tests/contract/test_program_bootstrap.py"
      provides: "Exact count, semantics, IDs, hashes, and drift regression tests"
  key_links:
    - from: "src/workout_tracker/contracts/program.py"
      to: "config/program-bootstrap.yaml"
      via: "safe strict load"
      pattern: "load_program_bootstrap"
    - from: "config/program-bootstrap.yaml"
      to: "4-day upper lower program/"
      via: "source_file/source_section and canonical definition_sha256"
      pattern: "source_file"
    - from: "src/workout_tracker/contracts/program.py"
      to: "config/schema.yaml"
      via: "exercise, UUID4-shaped ID, nullable, range and enum validation"
      pattern: "SourceSchema"
---

<objective>
Transcribe and validate the four exact versioned workout complexes for REQ-WBK-02.

Purpose: Bootstrap `Програма` deterministically without turning the YAML transcription into a competing authority.
Output: Strict 31-prescription bootstrap contract and complete regression suite.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-RESEARCH.md
@config/schema.yaml
@4-day upper lower program/README.md
@4-day upper lower program/01 upper strength.md
@4-day upper lower program/02 lower strength.md
@4-day upper lower program/03 upper hypertrophy.md
@4-day upper lower program/04 lower hypertrophy.md
@4-day upper lower program/05 progression and session rules.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-03-SUMMARY.md
</read_first>

<context>
Apply D-17 and D-18 from Plan 01's decision trace. The Markdown program files remain the bootstrap specification; `config/program-bootstrap.yaml` is a validated transcription and never overwrites historical operational prescriptions by itself.

<interfaces>
From Plan 01-03:
- `load_source_schema(path) -> SourceSchema`
- frozen strict-model conventions from `contracts.blueprint`
- source ID patterns and exact workout labels from `config/schema.yaml`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build and prove the complete audited program bootstrap</name>
  <files>config/schema.yaml, config/program-bootstrap.yaml, docs/architecture/DATA_MODEL.md, src/workout_tracker/contracts/program.py, tests/contract/test_program_bootstrap.py</files>
  <behavior>
    - Exact per-complex counts are 8/7/8/8 and total 31.
    - Every block, unequal paired-set count, unilateral prescription, duration prescription, optional block, RIR interval, known rest, equipment and setup matches the cited Markdown source; source omissions remain NULL.
    - Lower Hypertrophy C1 resolves only to the dumbbell variant/equipment/setup/cohort per D-18.
    - Duplicate IDs, mutable-text/row-position ID derivation, changed definition under the same program version, and missing source hashes are rejected.
    - The fixture loads only when all schema and cross-row invariants pass.
    - Deterministic initial IDs remain unchanged for the same pinned bootstrap logical key.
    - A semantic change under an existing `program_version_id`/`program_item_id` produces an explicit conflict rather than mutation.
  </behavior>
  <action>Follow RED→GREEN within this task: first write all 31 semantic expectations, then transcribe the versioned YAML and implement the loader until that exact suite is green. Count RED only when pytest collects the intended tests and a deterministic assertion fails for the specifically missing bootstrap behavior; import errors, collection errors, unavailable tools, malformed fixtures/configuration and unrelated pytest failures are setup defects that must be corrected before implementation continues. Enumerate all rows, not only aggregate counts; include the fourth bench set without a fake fourth curl, Bulgarian split squat as one `unilateral_both` logical set, side plank as duration per side, and optional Lower Strength D1. Bump `schema_version` from `1.0.0` to `1.1.0`: make `rest_seconds_min/max` nullable as a pair and make program-level `equipment_id`, `setup_id` and `comparison_cohort_id` nullable without changing the stricter performed-set fields. Update `DATA_MODEL.md`; require both rest bounds NULL or both present with min <= max, and require `comparison_cohort_id` NULL whenever program equipment or setup is NULL. Reject sentinel slugs `standard`, `unspecified`, `unknown`, assumed `bodyweight`, or values copied from another row. Lower Strength D1 has NULL rest/equipment/setup/cohort; Lower Hypertrophy Bulgarian split squat and Hammer curl preserve their source-known fields and leave missing equipment/setup/cohort NULL. Because the sources omit material setup for the prescriptions, do not manufacture setup/cohort IDs; later performed sets remain responsible for actual comparison semantics. Store one active `program_version_id`, 31 source-owned `program_item_id` values, canonical source file/section locators, semantic keys, prescription fields, and SHA-256 definition hashes. Use a pinned namespace plus bootstrap version/workout/block/order semantic key for deterministic IDs and force RFC UUID version/variant bits; exclude row number and mutable exercise text. Per D-18, encode Lower Hypertrophy C1 exactly as `Dumbbell Romanian deadlift` with dumbbell-specific variant and source-known dumbbell equipment; absent material setup/cohort remains NULL, and a barbell variant requires another program version.</action>
  <verify>
    <automated>uv run pytest -q tests/contract/test_program_bootstrap.py</automated>
  </verify>
  <done>The final green suite proves all four complexes and 31 prescriptions exactly, with stable IDs, immutable version semantics, source locators, definition hashes, and the D-18 Dumbbell RDL.</done>
  <acceptance_criteria>
    - Counts are exactly 8, 7, 8, 8.
    - Reps/duration, laterality, pairs, RIR, known rest/equipment/setup, and optionality match source; omitted rest/equipment/setup/cohort is represented only as NULL under schema 1.1.0.
    - Used-version drift fails closed and requires a new program version.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Markdown/bootstrap YAML → operational projection | Human-readable prescriptions become typed Sheet rows |
| semantic key → stable source ID | Identity is generated once and must not follow row position/text |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Tampering | program transcription | high | mitigate | exhaustive 31-row source-locator/hash matrix and exact cross-contract validation |
| T-01-05 | Spoofing | deterministic source IDs | high | mitigate | pinned namespace, UUID4-shaped bits, semantic keys excluding row/text, uniqueness tests |
| T-01-06 | Tampering | used program version | high | mitigate | definition-hash conflict blocks mutation and requires a new version/cohort |
| T-01-07 | Information Disclosure | program fixture | low | accept | fixture contains only public repository prescriptions and stable synthetic IDs; tests reject personal facts and locators |
</threat_model>

<verification>
Run `uv run pytest -q tests/contract/test_program_bootstrap.py`, parse both YAML contracts with `yaml.safe_load`, and run `git diff --check`.
</verification>

<success_criteria>
- REQ-WBK-02 is represented by all four exact complexes and 31 exact prescriptions.
- D-18 is enforced as a version/cohort boundary.
- The bootstrap remains reproducible and cannot silently revise an operational version.
</success_criteria>

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `config/schema.yaml` | `schema_version` 1.1.0, paired nullable prescribed-rest fields, and nullable program equipment/setup/cohort without weakening performed-set facts |
| `docs/architecture/DATA_MODEL.md` | schema 1.1.0 nullable program-prescription semantics and cohort guard |
| `config/program-bootstrap.yaml` | `program_bootstrap_version`, one active program version, 31 typed prescriptions, source locators and hashes |
| `src/workout_tracker/contracts/program.py` | `ProgramBootstrap`, `ProgramPrescription`, `load_program_bootstrap`, deterministic ID/hash validators |
| `tests/contract/test_program_bootstrap.py` | exhaustive program bootstrap contract suite |

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-04-SUMMARY.md` when done.
</output>
