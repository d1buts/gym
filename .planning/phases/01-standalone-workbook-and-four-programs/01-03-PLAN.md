---
phase: 01-standalone-workbook-and-four-programs
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - src/workout_tracker/adapters/port.py
  - src/workout_tracker/adapters/memory.py
  - config/workbook-blueprint.yaml
  - tests/contract/test_gateway_contract.py
  - tests/unit/test_topology.py
autonomous: true
requirements:
  - REQ-WBK-01
must_haves:
  truths:
    - "The strict blueprint describes exactly seven managed tabs in this order: Старт, Програма, Сесії, Підходи, Рекомендації, Довідники, Дашборд."
    - "The four authoritative tabs reference config/schema.yaml; support tabs are explicitly non-authoritative."
    - "An unowned extra tab is preserved and reported, while only the initial default blank tab is removable during explicit clean initialization."
    - "The exact topology is observable and applicable through a credential-free in-memory gateway without using row position or Google object IDs as identity."
  artifacts:
    - path: "config/workbook-blueprint.yaml"
      provides: "Versioned exact managed workbook topology"
      contains: "workbook_contract_version"
    - path: "src/workout_tracker/adapters/port.py"
      provides: "Credential-free workbook gateway boundary"
      exports: ["WorkbookGateway", "ObservedWorkbook", "ChangePlan", "ApplyResult"]
    - path: "src/workout_tracker/adapters/memory.py"
      provides: "Offline gateway implementation for shared behavior tests"
      exports: ["InMemoryWorkbookGateway"]
    - path: "tests/unit/test_topology.py"
      provides: "Canonical exact-seven-tab topology assertions"
  key_links:
    - from: "config/workbook-blueprint.yaml"
      to: "src/workout_tracker/contracts/blueprint.py"
      via: "strict load through Plan 01-02 contracts"
      pattern: "workbook_contract_version"
    - from: "tests/contract/test_gateway_contract.py"
      to: "src/workout_tracker/adapters/memory.py"
      via: "shared WorkbookGateway behavior executes without Google"
      pattern: "InMemoryWorkbookGateway"
---

<objective>
Deliver the exact managed workbook topology for REQ-WBK-01.

Purpose: Prove the seven-tab authority boundary through a portable offline gateway before later plans add program, input, formulas, reconciliation, or Google transport.
Output: Exact topology blueprint, stable gateway port, in-memory seam, and topology/gateway tests.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@docs/specs/SPEC-GOOGLE-SHEETS-WORKBOOK.md
@docs/architecture/DATA_MODEL.md
@config/schema.yaml
@.planning/phases/01-standalone-workbook-and-four-programs/01-02-SUMMARY.md
</read_first>

<context>
Implement D-13, D-14 and D-15 where they govern the topology. Pin workbook properties to locale `uk_UA` and timezone `America/New_York` per D-13; do not infer either from the execution host.

<interfaces>
From Plan 01-02:
- `WorkbookBlueprint`, `TabBlueprint`, `ManagedObjectBlueprint`
- `load_source_schema(path)` and `load_workbook_blueprint(path, source_schema)`

Create:
- `WorkbookGateway.observe() -> ObservedWorkbook`
- `WorkbookGateway.apply(plan: ChangePlan) -> ApplyResult`

`ObservedWorkbook` carries normalized managed structural state and a managed fingerprint. API IDs remain observed attributes, never logical identity.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the exact seven-tab topology</name>
  <files>config/workbook-blueprint.yaml, tests/unit/test_topology.py</files>
  <behavior>
    - The blueprint contains the seven exact Ukrainian titles once and in required order.
    - The four authoritative tabs point to config/schema.yaml; support tabs are explicitly non-authoritative.
    - Locale is exactly uk_UA and timezone is exactly America/New_York.
    - Every managed tab has a stable logical key independent of title order, row position, and Google object ID.
  </behavior>
  <action>Write topology assertions first. RED is valid only after the intended tests collect and fail on an assertion for the specifically missing topology behavior; unavailable tools, import or collection errors, malformed fixtures/configuration, and unrelated failures are setup defects that must be corrected before implementation continues. Then create the versioned blueprint root and exact tab topology per D-13 through D-15. Use stable logical keys such as `tab:start`, explicit authority/ownership markers, and contract/schema/formula/bootstrap version references. Pin `uk_UA` and `America/New_York` as desired workbook properties and test their exact values. Leave detailed program rows, UX, formulas, and charts to their owning plans.</action>
  <verify>
    <automated>uv run pytest -q tests/unit/test_topology.py tests/contract/test_contract_loading.py</automated>
  </verify>
  <done>The desired workbook topology is exact, versioned, explicitly localized, and free of positional identity.</done>
  <acceptance_criteria>
    - Exactly seven managed tabs exist in required order.
    - Locale/timezone are explicit desired properties.
    - All tab ownership and authority roles validate against the source schema.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the offline gateway seam and non-destructive policy</name>
  <files>src/workout_tracker/adapters/port.py, src/workout_tracker/adapters/memory.py, tests/contract/test_gateway_contract.py, tests/unit/test_topology.py</files>
  <behavior>
    - The in-memory gateway implements observe/apply without importing Google libraries or reading environment secrets.
    - An extra unowned tab returns UNMANAGED_TAB_PRESENT and is not silently removed.
    - The sole default blank tab may be removed only during explicit clean initialization.
    - Managed identity and fingerprints ignore mutable row/order/API identifiers.
  </behavior>
  <action>Write the gateway contract cases first. RED is valid only after the intended tests collect and fail on an assertion for the specifically missing gateway behavior; unavailable tools, import or collection errors, malformed fixtures/configuration, and unrelated failures are setup defects that must be corrected before implementation continues. Create the gateway interfaces and a bounded in-memory implementation sufficient for shared contract tests. Store stable logical keys and canonical managed fingerprints. Model clean initialization separately from reconciliation so only the initial empty default tab can be removed under D-15; arbitrary user tabs remain untouched and produce an explicit conflict/status. Tests must prove gateway isolation, exact topology, clean initialization, unowned-object preservation, and no use of row number, chart/rule index, or Google ID as logical identity.</action>
  <verify>
    <automated>uv run pytest -q tests/unit/test_topology.py tests/contract/test_gateway_contract.py</automated>
  </verify>
  <done>The exact topology is independently observable/applicable offline and unowned workbook state is never silently destroyed.</done>
  <acceptance_criteria>
    - The clean-workbook exception is restricted to the single empty default tab.
    - Gateway imports and tests require no Google package state, credential, or locator.
    - Mutable positions and provider IDs are never entity keys.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| desired topology → gateway port | Validated state becomes planned mutation |
| observed workbook → managed normalization | Provider/user objects may be malformed or unowned |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-06 | Tampering/Denial | topology reconciliation | medium | mitigate | stable owner keys, explicit clean-init exception, and unowned-object conflict tests |
| T-01-07 | Spoofing | workbook object identity | high | mitigate | logical keys are independent of row/order/API IDs and are covered by gateway tests |
| T-01-08 | Information Disclosure | observed state | medium | mitigate | normalized state contains managed structure/hash only and excludes locator, credentials and cell payloads |
</threat_model>

<verification>
Run `uv run pytest -q tests/unit/test_topology.py tests/contract/test_contract_loading.py tests/contract/test_gateway_contract.py`, parse `config/schema.yaml` and `config/workbook-blueprint.yaml` with `yaml.safe_load`, and run `git diff --check`.
</verification>

<success_criteria>
- REQ-WBK-01 has one exact requirement owner and passing offline proof.
- Later plans can import stable gateway/contracts without redefining topology semantics.
- Unowned state fails visibly without arbitrary deletion.
</success_criteria>

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | Idempotently created mobile-friendly standalone workbook with four programs, formulas and dashboard | 01-01..01-09 | COVERED |
| REQ | REQ-WBK-01 | Exact seven-tab workbook | 01-03 | COVERED |
| REQ | REQ-WBK-02 | Four exact versioned complexes | 01-04 | COVERED |
| REQ | REQ-WBK-03 | Mobile input, validation, hints, formats and protections | 01-05 | COVERED |
| REQ | REQ-WBK-04 | Contract formulas/dashboard with explicit missing/incomparable states | 01-06 | COVERED |
| REQ | REQ-WBK-05 | Repeatable no-duplicate setup | 01-07 | COVERED |
| RESEARCH | — | Official uv preflight and approved canonical dependency policy | 01-01 | COVERED |
| RESEARCH | — | Strict desired state, pure reconcile, memory and Google gateways | 01-02, 01-03, 01-07, 01-08 | COVERED |
| RESEARCH | — | 31-row typed bootstrap with source locators/hashes | 01-04 | COVERED |
| RESEARCH | — | Mobile presentation, validation, owner markers and protections | 01-05 | COVERED |
| RESEARCH | — | Shared metric fixtures and formula registry | 01-06 | COVERED |
| RESEARCH | — | Safe observe/diff/apply/re-observe and fail-closed live UAT | 01-07..01-09 | COVERED |
| CONTEXT | D-01..D-04 | Start/navigation/manual mobile workflow | 01-05, 01-09 | COVERED |
| CONTEXT | D-05..D-08 | Visual system and dashboard layout | 01-05, 01-06, 01-09 | COVERED |
| CONTEXT | D-09..D-13 | Manual input, identity, NULL and exact locale/timezone | 01-03, 01-05 | COVERED |
| CONTEXT | D-14..D-15 | Blueprint and non-destructive reconciliation | 01-02, 01-03, 01-07 | COVERED |
| CONTEXT | D-16 | Pinned formulas and canonical fixtures | 01-06, 01-09 | COVERED |
| CONTEXT | D-17..D-18 | Program bootstrap and exact Dumbbell RDL | 01-04 | COVERED |
| CONTEXT | D-19 | Installed-app OAuth, disposable test-Sheet UAT and external secrets | 01-08, 01-09 | COVERED |
| CONTEXT | D-20 | Approved uv dependency set and no redundant unchanged-package checkpoint | 01-01 | COVERED |

Deferred ChatGPT capture, coherent SQLite mirror/query, and evidence-backed recommendations are excluded because CONTEXT.md assigns them to later phases.

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `config/workbook-blueprint.yaml` | exact managed topology, locale/timezone and version roots |
| `src/workout_tracker/adapters/port.py` | `WorkbookGateway`, `ObservedWorkbook`, `ChangePlan`, `ApplyResult` |
| `src/workout_tracker/adapters/memory.py` | initial `InMemoryWorkbookGateway` |
| `tests/contract/test_gateway_contract.py` | shared offline gateway contract |
| `tests/unit/test_topology.py` | exact seven-tab, properties and non-destructive topology tests |

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-03-SUMMARY.md` when done.
</output>
