---
phase: 01-standalone-workbook-and-four-programs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - uv.lock
  - src/workout_tracker/__init__.py
  - tests/conftest.py
  - tests/contract/test_dependency_policy.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "A fresh host without uv can bootstrap the current official uv release before any project command runs."
    - "The only direct packages are the user-approved D-20 set: pydantic, PyYAML, google-api-python-client, google-auth, and pytest."
    - "Canonical package names and authoritative source projects are checked before resolution; unchanged approved packages continue without another human checkpoint, while drift fails closed."
    - "The locked Python 3.12+ environment imports all approved packages without credentials, a Sheet locator, personal data, or a Google call."
  artifacts:
    - path: "pyproject.toml"
      provides: "Python 3.12+ package metadata, approved dependency declarations, pytest paths and google_uat marker"
      contains: "requires-python"
    - path: "uv.lock"
      provides: "Exact resolution of the approved direct dependency set"
    - path: "tests/contract/test_dependency_policy.py"
      provides: "Direct-package allowlist and canonical registry/source policy"
    - path: "tests/conftest.py"
      provides: "Credential-free repository test fixtures and default live-UAT skip policy"
  key_links:
    - from: "tests/contract/test_dependency_policy.py"
      to: "pyproject.toml"
      via: "asserts exact normalized direct dependency names and approved source mapping"
      pattern: "google-api-python-client"
    - from: "pyproject.toml"
      to: "uv.lock"
      via: "uv lock --check verifies the committed resolution"
      pattern: "requires-python"
---

<objective>
Bootstrap and lock the approved Python toolchain that supports all Phase 1 requirement plans.

Purpose: Remove the measured missing-uv blocker and enforce the package approval recorded in D-20 without asking the owner to re-approve unchanged canonical packages.
Output: Official uv bootstrap path, Python package skeleton, exact dependency policy, pytest infrastructure, and committed lockfile.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-RESEARCH.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-VALIDATION.md
</read_first>

<context>
This is a supporting infrastructure plan and intentionally has `requirements: []`. REQ-WBK-01 is owned exactly once by Plan 01-03; REQ-WBK-02..05 are owned by Plans 01-04..01-07.

D-20 records explicit user approval of the canonical direct set: `pydantic`, `PyYAML`, `google-api-python-client`, `google-auth`, and `pytest`. The earlier research audit marked them SUS only because download telemetry was unavailable; CONTEXT.md supersedes the proposed redundant checkpoint for these unchanged packages.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap uv from the official installer when absent</name>
  <files>pyproject.toml, src/workout_tracker/__init__.py</files>
  <action>Start with `command -v uv`. If it is absent as measured in RESEARCH.md, install uv using the current official Astral bootstrap documented at `https://docs.astral.sh/uv/getting-started/installation/` and fetched from `https://astral.sh/uv/install.sh`; fail on download, installer, binary-resolution, or `uv --version` error before creating the project environment. Do not use `pip`, `pipx`, an unpinned third-party wrapper, or `npx`. Create the Python 3.12+ package metadata and package version only after the resolved uv binary passes its preflight. Record the resolved uv version in execution evidence, not as a fabricated pin in prose.</action>
  <verify>
    <automated>command -v uv &gt;/dev/null &amp;&amp; uv --version &amp;&amp; uv python find '>=3.12'</automated>
  </verify>
  <done>A documented official uv binary is available and can resolve a Python 3.12+ interpreter before any lock/sync/test command runs.</done>
  <acceptance_criteria>
    - Missing uv triggers the official Astral bootstrap and a second binary preflight.
    - A bootstrap failure stops the plan before dependency resolution.
    - No credentials, locator, or personal data enter the bootstrap command or project files.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce the approved package policy and exact lock</name>
  <files>pyproject.toml, uv.lock, tests/conftest.py, tests/contract/test_dependency_policy.py</files>
  <behavior>
    - Direct runtime dependencies normalize exactly to pydantic, pyyaml, google-api-python-client, and google-auth; pytest is the only direct development dependency.
    - PyPI metadata names and source links match `pydantic/pydantic`, `yaml/pyyaml`, `googleapis/google-api-python-client`, `googleapis/google-cloud-python/tree/main/packages/google-auth`, and `pytest-dev/pytest`; the migrated `google-auth` mapping was separately owner-approved after the archived repository triggered the fail-closed checkpoint.
    - An unchanged canonical D-20 set proceeds without a human checkpoint; a changed name, source, registry, or extra direct package fails before lock generation.
    - Default test collection skips google_uat tests before authentication and performs no network request.
  </behavior>
  <action>Write the dependency-policy test first. RED is valid only after the intended tests collect and fail on an assertion for the specifically missing dependency-policy behavior; unavailable uv/tools, import or collection errors, malformed fixtures/configuration, and unrelated failures are setup defects that must be corrected before implementation continues. Per D-20 and the user's recorded package-legitimacy approval, validate each exact canonical PyPI project name and authoritative source URL against PyPI JSON/project metadata immediately before resolution. Continue automatically when all five mappings match; stop only on package-name, registry, source-project, or approved-set drift. Do not add the research-proposed human checkpoint for the unchanged approved set. Declare bounded compatible versions from RESEARCH.md, put pytest in the development group, generate `uv.lock`, then run locked sync. Configure unit/contract/UAT paths and register `google_uat`; ordinary collection must skip live cases without attempting auth, while the later dedicated live runner will fail closed when live execution is explicitly requested without opt-in.</action>
  <verify>
    <automated>uv lock --check &amp;&amp; uv sync --locked &amp;&amp; uv run pytest -q tests/contract/test_dependency_policy.py &amp;&amp; uv run python -c "import pydantic, yaml, google.auth, googleapiclient, pytest"</automated>
  </verify>
  <done>The approved canonical packages and sources are validated, exactly locked, importable, and covered by a regression policy without a redundant approval pause.</done>
  <acceptance_criteria>
    - `requires-python` is at least 3.12 and `uv.lock` matches `pyproject.toml`.
    - No unapproved direct dependency is present.
    - Default pytest collection needs no external state and makes no Google call.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| official installer endpoint → local uv binary | Bootstrap code enters the execution environment |
| PyPI metadata → approved dependency policy | Registry names/source links are checked against D-20 |
| dependency declarations → locked environment | Third-party code becomes importable |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | uv bootstrap | high | mitigate | official HTTPS installer endpoint, fail-closed installer/binary preflight, and recorded resolved version |
| T-01-SC | Tampering | uv/PyPI resolution | high | mitigate | D-20 exact allowlist, canonical PyPI/source metadata validation, exact lock, and immediate stop on drift |
| T-01-02 | Information Disclosure | pytest collection | medium | mitigate | live tests skip before auth by default and fixtures contain no locator, credential, notes, or personal rows |
</threat_model>

<verification>
Run the two task commands, then `uv run pytest -q --collect-only`, `git diff --check`, and confirm the dependency policy cites D-20 approval and contains exactly five direct package names.
</verification>

<success_criteria>
- A host matching the measured environment can install and preflight official uv before project commands.
- The unchanged user-approved dependency set resolves without another human checkpoint.
- Any package identity/source drift fails before lock generation.
</success_criteria>

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `pyproject.toml` | Python metadata, exact direct dependency declarations, pytest configuration and markers |
| `uv.lock` | exact approved dependency resolution |
| `src/workout_tracker/__init__.py` | package version |
| `tests/conftest.py` | repository fixtures and safe default UAT behavior |
| `tests/contract/test_dependency_policy.py` | D-20 allowlist and canonical package-source checks |

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-01-SUMMARY.md` when done.
</output>
