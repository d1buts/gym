---
phase: 01-standalone-workbook-and-four-programs
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/workout_tracker/contracts/__init__.py
  - src/workout_tracker/contracts/blueprint.py
  - src/workout_tracker/contracts/source_schema.py
  - src/workout_tracker/adapters/__init__.py
  - tests/contract/test_contract_loading.py
autonomous: true
requirements: []
must_haves:
  truths:
    - "Repository schema and workbook-blueprint YAML load only into frozen strict contracts."
    - "Unknown keys, missing versions, duplicate logical keys, row-position identity, invalid ownership, and unregistered formula text fail before any gateway can mutate state."
    - "Contract diagnostics expose codes, counts, and hashes without raw rows, notes, locators, or credentials."
  artifacts:
    - path: "src/workout_tracker/contracts/blueprint.py"
      provides: "Strict immutable WorkbookBlueprint contract"
      exports: ["WorkbookBlueprint", "TabBlueprint", "ManagedObjectBlueprint", "load_workbook_blueprint"]
    - path: "src/workout_tracker/contracts/source_schema.py"
      provides: "Strict source schema contract"
      exports: ["SourceSchema", "load_source_schema"]
    - path: "tests/contract/test_contract_loading.py"
      provides: "Fail-closed repository-contract matrix"
  key_links:
    - from: "src/workout_tracker/contracts/source_schema.py"
      to: "config/schema.yaml"
      via: "yaml.safe_load followed by strict typed validation"
      pattern: "safe_load"
    - from: "src/workout_tracker/contracts/blueprint.py"
      to: "config/workbook-blueprint.yaml"
      via: "cross-contract version, ownership and logical-key validation"
      pattern: "WorkbookBlueprint"
---

<objective>
Define the strict repository contracts that support the REQ-WBK-01 topology owner in Plan 01-03.

Purpose: Establish a fail-closed Pydantic boundary before topology, UX, formula, or Google work.
Output: Source-schema and workbook-blueprint contract models, public exports, and contract-loading tests.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@docs/specs/SPEC-GOOGLE-SHEETS-WORKBOOK.md
@docs/architecture/DATA_MODEL.md
@config/schema.yaml
@.planning/phases/01-standalone-workbook-and-four-programs/01-01-SUMMARY.md
</read_first>

<context>
This supporting contract plan intentionally has `requirements: []`; Plan 01-03 owns REQ-WBK-01 exactly once.

<interfaces>
Create:
- frozen immutable `WorkbookBlueprint`, `TabBlueprint`, `ManagedObjectBlueprint`
- `load_source_schema(path) -> SourceSchema`
- `load_workbook_blueprint(path, source_schema) -> WorkbookBlueprint`

Google discovery types must not cross this contract boundary.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define strict source and workbook contracts</name>
  <files>src/workout_tracker/contracts/__init__.py, src/workout_tracker/contracts/blueprint.py, src/workout_tracker/contracts/source_schema.py, src/workout_tracker/adapters/__init__.py, tests/contract/test_contract_loading.py</files>
  <behavior>
    - A valid schema/blueprint pair loads into frozen typed models.
    - Unknown/missing fields, invalid version references, duplicate logical keys, row-position identity, and formula text outside the Git registry fail before gateway apply.
    - Source facts, Sheet-calculated values, and managed ownership metadata remain distinct typed concepts.
  </behavior>
  <action>Write the failure matrix first. RED is valid only after the intended tests collect and fail on an assertion for the specifically missing contract behavior; unavailable tools, import or collection errors, malformed fixtures/configuration, and unrelated failures are setup defects that must be corrected before implementation continues. Then implement strict Pydantic models with `extra="forbid"` and frozen instances. Parse YAML only with `yaml.safe_load`. Validate contract/schema/formula/bootstrap version references, stable managed logical keys, exact authority roles, source-column references, and ownership policy before producing a model. Reject row numbers, mutable display positions, and Google object indexes as logical identity. Map failures to privacy-safe codes/counts/hashes and keep Google client imports outside these modules.</action>
  <verify>
    <automated>uv run pytest -q tests/contract/test_contract_loading.py</automated>
  </verify>
  <done>Invalid repository contracts fail closed before topology compilation or gateway access.</done>
  <acceptance_criteria>
    - YAML construction is safe and typed.
    - Logical keys and referenced versions are validated before use.
    - Diagnostics contain no raw payload, notes, locator, credential path, or token.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository YAML → Pydantic contracts | Untrusted text becomes executable desired-state input |
| typed contracts → later compilers/gateways | Invalid semantics must stop before mutation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-03 | Tampering/Elevation | YAML loaders | high | mitigate | `safe_load`, frozen extra-forbid models, version allowlists, ownership validation, and failure-matrix tests |
| T-01-04 | Spoofing | managed identity | high | mitigate | stable logical-key validation and explicit rejection of row/index/title identity |
| T-01-05 | Information Disclosure | validation errors | medium | mitigate | errors expose codes/counts/hashes only; contract tests reject sensitive fields |
</threat_model>

<verification>
Run `uv run pytest -q tests/contract/test_contract_loading.py`, parse `config/schema.yaml` with `yaml.safe_load`, and run `git diff --check`.
</verification>

<success_criteria>
- Downstream plans receive stable typed interfaces without reinterpreting repository YAML.
- Invalid identity, ownership, formula, and version state cannot reach a gateway.
</success_criteria>

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `src/workout_tracker/contracts/__init__.py` | public contract exports |
| `src/workout_tracker/contracts/blueprint.py` | workbook blueprint models and loader |
| `src/workout_tracker/contracts/source_schema.py` | source schema model and loader |
| `src/workout_tracker/adapters/__init__.py` | adapter package boundary |
| `tests/contract/test_contract_loading.py` | strict loading and privacy-safe diagnostics matrix |

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-02-SUMMARY.md` when done.
</output>
