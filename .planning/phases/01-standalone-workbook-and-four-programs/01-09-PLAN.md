---
phase: 01-standalone-workbook-and-four-programs
plan: 09
type: execute
wave: 9
depends_on:
  - 01-08
files_modified:
  - scripts/run_google_uat.py
  - tests/contract/test_google_uat_runner.py
  - tests/uat/conftest.py
  - tests/uat/test_clean_workbook.py
  - tests/uat/test_formula_results.py
  - tests/uat/test_second_run.py
autonomous: false
requirements: []
user_setup:
  - service: google_sheets_test
    why: "Required only for final live formula/rendering/mobile/second-run UAT on a disposable Google Spreadsheet"
    env_vars:
      - name: WORKOUT_TEST_SOURCE_ALIAS
        source: "Choose a non-production logical alias; never commit the Spreadsheet ID or URL"
      - name: WORKOUT_TEST_EXPECTED_TITLE
        source: "Exact disposable Spreadsheet title used by the target preflight"
      - name: WORKOUT_TEST_DISPOSABLE
        source: "Set to 1 only after confirming the target contains no personal or production data"
    dashboard_config:
      - task: "Store the disposable target locator and installed-app OAuth client/token in local untracked configuration with restrictive permissions"
        location: "Local machine outside Git; do not pass secrets or the locator as CLI arguments"
      - task: "Authorize only the minimum Google Sheets scope for the disposable target"
        location: "Local installed-app OAuth flow; do not grant Drive sharing or permission mutation"
must_haves:
  truths:
    - "The final live runner validates only fixed desired locale uk_UA/timezone America/New_York, allows a clean disposable target with different observed defaults to be reconciled, and requires exact desired properties after apply and re-observation."
    - "A live-UAT invocation fails if zero non-skipped google_uat tests execute, even when pytest itself would otherwise exit successfully because every selected case skipped."
    - "The runner records a privacy-safe executed-test count and final pass/fail status without test values, test names containing sensitive aliases, Spreadsheet IDs/URLs, credentials, paths, notes, or raw Google errors."
    - "Live assertions prove exact clean setup, canonical formula results and NULL/status behavior, and an empty second ChangePlan with an unchanged logical hash."
    - "Manual UAT explicitly verifies dashboard cards, textual statuses, filters, chart series and visible NULL gaps against the same non-personal canonical fixtures."
  artifacts:
    - path: "scripts/run_google_uat.py"
      provides: "Fail-closed live-UAT wrapper with fixed desired-property validation, safe target preflight and non-zero executed-count enforcement"
    - path: "tests/uat/conftest.py"
      provides: "google_uat execution counter and privacy-safe terminal evidence hook"
    - path: "tests/contract/test_google_uat_runner.py"
      provides: "Regression proof that all-skipped live selection fails and non-skipped execution is counted safely"
    - path: "tests/uat/test_clean_workbook.py"
      provides: "Live exact topology, program, locale/timezone and managed-object assertions"
    - path: "tests/uat/test_formula_results.py"
      provides: "Live canonical formula, dashboard, status, filter and chart-series assertions"
    - path: "tests/uat/test_second_run.py"
      provides: "Live empty-second-plan and unchanged-logical-hash proof"
  key_links:
    - from: "scripts/run_google_uat.py"
      to: "tests/uat/conftest.py"
      via: "selects only google_uat cases and treats an executed count of zero as failure"
      pattern: "google_uat"
    - from: "tests/uat/test_formula_results.py"
      to: "tests/fixtures/metrics-v1.yaml"
      via: "loads the canonical non-personal cases used by the offline oracle"
      pattern: "metrics-v1"
    - from: "tests/uat/test_second_run.py"
      to: "src/workout_tracker/cli.py"
      via: "applies once, re-observes, invokes setup again and asserts zero operations plus stable logical hash"
      pattern: "ChangePlan"
---

<objective>
Close Phase 1 with privacy-safe live Google UAT and an explicit human rendering/mobile checkpoint.

Purpose: Supply the final transport/rendering evidence for D-01 through D-08, D-13, D-16 and D-19 without duplicating REQ-WBK-01..05 ownership or modifying production code already owned by Plans 01-03..01-08.
Output: A fail-closed live-UAT runner, live-only test modules, privacy-safe execution evidence and a blocking manual verification checkpoint.
</objective>

<execution_context>
@/home/muuser/.codex/gsd-core/workflows/execute-plan.md
@/home/muuser/.codex/gsd-core/templates/summary.md
</execution_context>

<read_first>
@AGENTS.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-CONTEXT.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-RESEARCH.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-VALIDATION.md
@docs/specs/SPEC-GOOGLE-SHEETS-WORKBOOK.md
@docs/architecture/METRICS.md
@tests/fixtures/metrics-v1.yaml
@.planning/phases/01-standalone-workbook-and-four-programs/01-06-SUMMARY.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-07-SUMMARY.md
@.planning/phases/01-standalone-workbook-and-four-programs/01-08-SUMMARY.md
</read_first>

<context>
This is a support/UAT plan and intentionally has `requirements: []`. Requirement ownership remains exact once: REQ-WBK-01 in 01-03, REQ-WBK-02 in 01-04, REQ-WBK-03 in 01-05, REQ-WBK-04 in 01-06 and REQ-WBK-05 in 01-07.

Plan 01-08 already owns all production Google adapter, request compiler and CLI files. This plan may consume those interfaces but must not modify them. Per D-13, the only accepted initial workbook properties are locale `uk_UA` and timezone `America/New_York`; neither is configurable in this UAT plan. Per D-19, live calls require local installed-app OAuth, minimum Sheets scope, untracked locator resolution and an explicitly disposable target.
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create a fail-closed live-UAT runner and execution counter</name>
  <files>scripts/run_google_uat.py, tests/uat/conftest.py, tests/contract/test_google_uat_runner.py</files>
  <behavior>
    - The wrapper selects the three declared UAT modules with the google_uat marker and runs the Plan 01-08 target/credential preflight before any Google call.
    - Missing opt-in, unsafe/disallowed target identity or disposability, unsafe credential permissions, missing credentials, invalid fixed desired constants, or an all-skipped selection returns non-zero and performs no mutation; observed locale/timezone mismatch alone is permitted managed drift.
    - Exactly uk_UA and America/New_York are required constants; environment or host values cannot replace them.
    - The executed count includes each selected google_uat test once only when its call phase actually ran, excludes collection errors and skipped cases, and is greater than zero for a successful run.
    - Terminal/evidence output contains only the logical source alias, contract/version hashes, managed counts/change kinds, `google_uat_executed_count`, redacted error code and pass/fail.
  </behavior>
  <action>Write the runner regression tests before implementation. A valid RED is only a failing assertion that names one missing runner behavior; import errors, collection errors, unavailable tools, malformed configuration and unrelated pytest failures are setup defects and must be corrected before continuing. Implement a small wrapper around the exact live pytest selection and a pytest hook/plugin that counts one call-phase report per non-skipped `google_uat` item. Preserve pytest's failure exit status, and additionally force a non-zero exit when the executed count is zero so a fully skipped selection can never satisfy UAT. Run the Plan 01-08 gate in its required order: validate fixed desired locale `uk_UA` and timezone `America/New_York` locally, enforce credential file/directory permissions, authenticate, verify exact target identity/title/disposability, and allow observed locale/timezone differences to proceed as managed drift. Emit the executed count and pass/fail through the existing redaction boundary; never emit node IDs, raw parameters, target identifiers, credential/config paths or raw exceptions. Contract tests must simulate all-skipped, collection-error, one-pass and one-fail outcomes plus a safe clean-target property mismatch without authenticating or making a network request.</action>
  <verify>
    <automated>uv run pytest -q tests/contract/test_google_uat_runner.py</automated>
  </verify>
  <done>The dedicated live runner cannot report success unless at least one marked live test actually executes and all executed tests pass, and its count/status evidence is privacy-safe.</done>
  <acceptance_criteria>
    - A zero executed count is an explicit failure regardless of pytest's ordinary skip exit code.
    - Invalid or inferred desired locale/timezone constants fail locally; different observed clean-target defaults do not fail preflight and exact post-state values are required.
    - Contract verification runs without a Google credential, locator or network call.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Define complete clean/formula/second-run live assertions</name>
  <files>tests/uat/test_clean_workbook.py, tests/uat/test_formula_results.py, tests/uat/test_second_run.py</files>
  <behavior>
    - Clean setup proves exact uk_UA/America/New_York properties, seven exact ordered tabs, four complexes, 31 items, exact Dumbbell Romanian deadlift at Lower Hypertrophy C1, and all managed named ranges/validations/protections/formulas/filter views/charts.
    - A clean disposable target starting with different locale/timezone defaults is successfully reconciled to exact uk_UA/America/New_York and re-observed before success.
    - Formula UAT loads the same non-personal metrics-v1 fixtures as the offline oracle and proves displayed value, unit, textual status and exclusion reason for normal, missing, invalid and incomparable cases.
    - Dashboard assertions cover every summary card, filter definition and chart domain/series; incompatible cohorts never share a scalar/series and missing values remain NULL gaps with interpolation disabled.
    - Second setup returns an empty ChangePlan, zero operations, unchanged logical hash and no duplicate managed object or program/reserve ID.
  </behavior>
  <action>Define the three `google_uat` modules as consumers of the production interfaces completed in 01-08; do not add or modify production behavior in this plan. A valid RED is only a deterministic assertion mismatch against the declared live contract; authentication, target preflight, import, collection and configuration failures are not acceptable RED evidence. Reuse `tests/fixtures/metrics-v1.yaml` rather than inventing a second fixture set. Make the clean test exercise initialization from a disposable target whose observed locale/timezone differ, then assert apply and re-observation produce exact `uk_UA`/`America/New_York` plus all exact managed objects. Make the formula test wait with a bounded recalculation poll, then compare displayed values/units/status/reasons and dashboard helper ranges/chart specs to canonical expectations, including missing-load, incomplete-unilateral, warm-up/skipped/void, mixed-cohort, machine-level, assistance/bodyweight, e1RM 1/12/13, Decimal tie, paired-rest and missing-RIR cases. Make the second-run test compare normalized managed state, every persisted reserve ID, operation count and logical hash rather than provider-generated object IDs or formatting noise. All failures must be passed through the redacted error boundary.</action>
  <verify>
    <automated>uv run pytest -q tests/contract/test_google_uat_runner.py &amp;&amp; uv run pytest -q --collect-only tests/uat/test_clean_workbook.py tests/uat/test_formula_results.py tests/uat/test_second_run.py</automated>
  </verify>
  <done>The complete live test selection is collectible offline, consumes canonical fixtures and is ready for the fail-closed disposable-target runner.</done>
  <acceptance_criteria>
    - The UAT modules own only test assertions and do not duplicate production implementations.
    - Canonical missing/incomparable cases expect NULL plus explicit status/reason, never synthetic zero.
    - Default collection performs no authentication or Google request.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify the disposable workbook, dashboard and narrow-mobile journey</name>
  <files>scripts/run_google_uat.py, tests/uat/test_clean_workbook.py, tests/uat/test_formula_results.py, tests/uat/test_second_run.py</files>
  <action>Pause only after the full offline suite and the runner contract pass. Resolve the approved installed-app OAuth and disposable target outside Git, confirm the source alias/title/disposable marker, credential file mode `0600`, and containing-directory mode `0700`, then let the executor run the dedicated wrapper. The target may begin with provider-default locale/timezone values; the wrapper result is acceptable only when reconciliation updates them, post-apply re-observation proves exact `uk_UA`/`America/New_York`, reports a non-zero `google_uat_executed_count`, and overall pass. After automated live assertions pass, perform the manual checks below against the same non-personal canonical fixtures. If safe runtime inputs are unavailable, leave the checkpoint pending; do not weaken, mock, redirect or claim the live evidence.</action>
  <what-built>A support-only live-UAT suite for the already implemented workbook: exact clean setup, canonical formula/dashboard behavior, protected mobile input and true second-run no-op.</what-built>
  <how-to-verify>
    1. Confirm the target is disposable, contains no personal/production data, uses installed-app OAuth with minimum Sheets scope, has credential/token files mode `0600` inside directories mode `0700`, and has no Drive sharing/permission mutation. Keep its ID/URL, token and local paths outside Git and terminal arguments.
    2. Run `uv run python scripts/run_google_uat.py` against a clean disposable target whose provider defaults differ from at least one desired property. Confirm the fixed desired values remain `uk_UA` and `America/New_York`, the mismatch is planned/applied as managed drift, post-apply re-observation proves both exact values, `google_uat_executed_count` is greater than zero, and final status is pass without exposing sensitive fields.
    3. Confirm the live assertions passed for exact seven-tab order, four complexes/31 program items, exact Dumbbell Romanian deadlift, managed validations/protections/named ranges/filter views/formulas/charts, canonical formula results and empty second setup with unchanged logical hash.
    4. On `Дашборд`, compare every compact summary card to the canonical fixture expectations: sessions by week/type, working-set count, rep/load volume with unit, best eligible e1RM, target-versus-actual RIR, completion/data-quality, deterministic progression status and recommendation lifecycle status. Confirm unavailable values are blank/NULL and have adjacent Ukrainian text/symbol status rather than color-only meaning.
    5. Exercise the workout type, date, exact exercise variant and comparison-cohort filters individually and together. Confirm cards/helper ranges/chart series contain only matching fixture rows and never combine incompatible variant, equipment, setup, load basis, assistance semantics, cohort or unit family.
    6. Inspect each contract-defined chart series against the canonical fixture/helper range: domain and series ranges, header count, unit/cohort label and textual status must match. Confirm missing/incomparable fixture points render as visible NULL gaps and are not joined or coerced to zero.
    7. On a phone or narrow Google Sheets viewport, verify `Старт` shows the next complex, last completed session, four one-tap links and redacted sync/backup status; open all four complexes; activate a non-personal test session before its child set; enter fixture facts without horizontal scrolling through primary fields; verify hints/formats/invalid-or-blank text statuses and inability to edit protected system/formula columns.
    8. Record only source alias, contract/version hashes, managed counts/change kinds, executed-test count, redacted error code and pass/fail. Do not record Spreadsheet IDs/URLs, credentials/paths, workout values, notes, symptoms or raw Google errors.
  </how-to-verify>
  <verify>
    <automated>uv run python scripts/run_google_uat.py</automated>
    <human-check>Dashboard cards, textual statuses, filters, chart series, NULL gaps and the narrow-mobile workflow match the canonical fixtures, and the privacy-safe live evidence reports a non-zero executed count plus pass.</human-check>
  </verify>
  <resume-signal>Type "approved" with only the redacted count/hash/pass summary, or identify the failed numbered step without including target identifiers, credentials, paths, fixture values, notes, symptoms or raw Google errors.</resume-signal>
  <done>Disposable Google clean setup, canonical formula/dashboard rendering, protected mobile workflow and second-run no-op are proven by non-skipped live tests and explicit privacy-safe human UAT.</done>
  <acceptance_criteria>
    - The checkpoint cannot pass when all live tests skip or when safe local authorization is unavailable.
    - Manual evidence explicitly covers cards, text statuses, filters, chart series and NULL gaps against canonical fixtures.
    - No production workbook, sharing setting, sensitive locator, credential or personal fact is touched or recorded.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local runtime/OAuth → existing Google adapter | Test locator and installed-app credentials enter only after preflight |
| pytest selection → UAT evidence | Skipped/failed/executed state must be counted accurately without leaking parameters |
| Google cells/charts → human verification | Rendered state may differ from request snapshots and must be checked against canonical fixtures |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-25 | Spoofing/Tampering | live target preflight | critical | mitigate | explicit disposable marker/alias/title, fixed desired uk_UA/America/New_York, managed property update plus exact post-state check, installed-app OAuth, minimum Sheets scope, 0600/0700 credential boundary and no Drive mutation |
| T-01-26 | Repudiation | skipped live selection | high | mitigate | call-phase execution counter, privacy-safe executed-count evidence and forced failure when count is zero |
| T-01-27 | Tampering | formula/dashboard rendering | high | mitigate | canonical metrics-v1 fixtures, bounded recalculation, exact value/unit/status/filter/chart/NULL-gap assertions and manual comparison |
| T-01-28 | Information Disclosure | UAT output/evidence | high | mitigate | redaction before rendering and an allowlist limited to alias/hash/count/kind/code/pass-fail fields |
| T-01-29 | Denial | unavailable safe live environment | low | accept | checkpoint remains pending without weakening or fabricating required evidence |
</threat_model>

<verification>
Before the checkpoint, run `uv run pytest -q`, `uv run pytest -q tests/contract/test_google_uat_runner.py`, parse every YAML with `yaml.safe_load`, run `git diff --check`, verify relative Markdown links, scan tracked text for credentials/live locators/prohibited personal data, and confirm each Phase 1 requirement appears in exactly one plan frontmatter. The live gate is only `uv run python scripts/run_google_uat.py`; a direct pytest command with all cases skipped is not completion evidence.
</verification>

<success_criteria>
- Plan 01-09 owns no production files and no Phase 1 requirement ID.
- Fixed desired uk_UA/America/New_York validation, clean-target managed-property initialization, exact post-state verification and the non-zero executed-count gate prevent skipped or incomplete UAT from passing.
- Automated live tests prove clean setup, canonical formulas/dashboard structures and second-run no-op.
- Manual UAT explicitly proves dashboard cards, textual statuses, filters, chart series, NULL gaps and narrow-mobile use against canonical fixtures.
</success_criteria>

## Artifacts this phase produces

| New file | Symbols / content introduced by this plan |
|----------|-------------------------------------------|
| `scripts/run_google_uat.py` | dedicated preflighted runner and final privacy-safe UAT result |
| `tests/contract/test_google_uat_runner.py` | zero-execution, skip, failure and evidence-redaction runner tests |
| `tests/uat/conftest.py` | non-skipped google_uat execution counter |
| `tests/uat/test_clean_workbook.py` | exact live topology/program/properties/managed-object assertions |
| `tests/uat/test_formula_results.py` | canonical live formula/dashboard/filter/chart/NULL-gap assertions |
| `tests/uat/test_second_run.py` | empty second ChangePlan and unchanged logical hash proof |

<output>
Create `.planning/phases/01-standalone-workbook-and-four-programs/01-09-SUMMARY.md` when done.
</output>
